/*#include <thread>
#include "botSocket.hpp"

enum SockFlow{
    IN = 1,
    OUT = 0
};

class sockThread{
    public:               
        sockThread();
        thread start();
        
    private:
        int direciton;
        botSocket socket;

};*/